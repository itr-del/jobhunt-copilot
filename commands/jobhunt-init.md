---
description: 初始化求职工作区（建目录、拷模板、注册 skill 入口）
---

读取并严格按 `skills/jobhunt-init/SKILL.md` 的全流程执行：检查前置依赖 → 先看后动（已有工作区只补缺不覆盖）→ 在用户指定位置创建工作区 → 拷贝模板 → 注册三个项目级 skill 入口 → 引导下一步走 career-grill。

用户补充：$ARGUMENTS
